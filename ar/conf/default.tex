% Font and language setup
\usepackage{fontspec}
\usepackage[english,
            bidi=basic,
            headfoot=arabic,
            layout=counters.tabular]{babel}
\babelprovide[import, main,
            justification=kashida,
            transforms = kashida.plain]{arabic}
\setmainfont[Ligatures=TeX]{LatinModernRoman}
\babelfont[arabic]{rm}[Language=Default]{Amiri}
\defaultfontfeatures{Renderer=Harfbuzz}

% achievements environment with no item separation
\renewenvironment{achievements}
{
	\vspace*{0.2em}
	\begin{itemize}[topsep=0pt, noitemsep]
}
{
	\end{itemize}
	\vspace*{0.5em}
}
